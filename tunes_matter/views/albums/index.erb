<h1>All Albums <a class="link-near-heading" href="/albums/new">Add New Album</a></h1>

<hr/>

<% @albums.each do |album| %>

<p class="data-row">"<%= album.title %>" by <%= album.artist.name() %>  (<%= album.original_release_year %>) Genre: <%= album.genre %> <a class="navlink" href="/albums/<%=album.id%>"><%= album.all_editions.count %> Edition<%="s" if album.all_editions.count != 1 %></a> <a class="edit-link" href="/albums/<%= album.id%>/edit">EDIT</a> <form class="delete-form" action="/albums/<%= album.id %>/delete" method="POST">
  <input type="submit" class="delete-link" value="DELETE"></form> </p>
<% end %>

<hr/>
