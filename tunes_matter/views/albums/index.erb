<h2>All Albums</h2>

<hr/>

<% @album.each do |album| %>

<p class="data-row">"<%= album.title %>" by <%= album.artist.name() %>  (<%= album.original_release_year %>) Genre: <%= album.genre %> <a class="edit-link" href="/albums/<%= album.id%>/edit">EDIT</a> <form class="delete-form" action="/albums/<%= album.id %>/delete" method="POST">
  <input type="submit" class="delete-link" value="DELETE"></form> </p>
<% end %>

<hr/>
<a href="/albums/new">Add New Album</a>
<hr/>
