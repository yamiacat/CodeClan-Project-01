<h2>Edit Album</h2>

<form action="/albums/<%= @album.id %>" method="POST">

    <label for="artist-confirm">Confirm Artist:</label>
    <select id="artist-confirm" name="artist_id">

      <% @artists.each do |artist| %>
      <option <%= "selected" if @album.artist.name == artist.name %> value="<%= artist.id %>"> <%= artist.name %> </option>
      <% end %>
    </select>


      <label for="title-field">Album title:</label>
      <input id="title-field" name="title" type="text" value="<%= @album.title %>"/>


    <label for="genre-field">Album Genre:</label>
    <input id="genre-field" name="genre" type="text" value="<%= @album.genre %>"/>

    <label for="original-release-year-field">Original Release Year:</label>
    <input id="original-release-year-field" name="original_release_year" type="number" value="<%= @album.original_release_year %>"/>

  <input type="submit" value="Update Album Details">

</form>
